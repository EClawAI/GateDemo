server:
  port: 8081

spring:
  application:
    name: game-service

game:
  id: ${GAME_ID:1001}  # Game ID（数字），从启动参数读取
  cache:
    max-size: 10000
    expire-minutes: 30

grpc:
  port: ${GRPC_PORT:9090}  # gRPC 服务监听端口，从启动参数读取

logging:
  level:
    com.clawai.gatedemo: DEBUG