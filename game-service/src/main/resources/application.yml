server:
  port: 8081

spring:
  application:
    name: game-service

game:
  id: ${GAME_ID:game-1001}
  cache:
    max-size: 10000
    expire-minutes: 30

grpc:
  port: 9090  # gRPC 服务监听端口

logging:
  level:
    com.clawai.gatedemo: DEBUG