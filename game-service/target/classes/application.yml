server:
  port: 8081

spring:
  application:
    name: game-service

game:
  id: ${GAME_ID:game-1001}
  cache:
    max-size: 10000
    expire-minutes: 30

logging:
  level:
    com.clawai.gatedemo: DEBUG