server:
  port: 8082

spring:
  application:
    name: player-client

player:
  player-id: ${PLAYER_ID:100001}
  host: ${GATE_HOST:localhost}
  port: ${GATE_PORT:8888}
  heartbeat-interval: 30

logging:
  level:
    com.clawai.gatedemo: DEBUG