# Dev profile: TLS off, debug logging（Redis 密码见 application.yml / REDIS_PASSWORD）
gate:
  tls:
    enabled: false
  jwt:
    secret: ${JWT_SECRET:DefaultGateDemoSecretKeyForHMACSHA256Auth!}

logging:
  level:
    com.clawai.gatedemo: DEBUG
    org.springframework.web.socket: DEBUG
