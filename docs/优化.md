1. ("gate".equals(targetService)使用了字符串匹配，改为定义服务类型枚举，进行匹配
2. 消息的生成：通过shell脚本或者python脚本生成proto消息，同时生成一个json结构文件，其中包括，消息id、消息名字、消息处理目标服务（下行消息可以不标记），同时需要检查上行消息必须标记目标服务 
3. game、chat等服务启动时通过读取生成的消息json结构，将消息id和消息名字的双向映射注册到map中，在注册消息对应的handler时，通过消息名查询到对应的id，最终形成id->handler的映射，在给客户端传递消息时同样，在打包消息封装消息时，根据消息名字确认消息id，仅传输id，不需要传输消息名
4. 