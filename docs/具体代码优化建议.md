
TokenBlacklistService.java
代码优化建议
1. 参数验证增强:
   - 增加对 ttlSeconds 的有效性检查，确保其为正数 
   - 考虑对 jti 的格式进行验证，确保其符合 JWT ID 的格式要求
2. 监控与指标:
   - 添加 Redis 操作的监控指标，如黑名单添加成功率、检查成功率等 
   - 考虑添加黑名单大小的监控，以便及时发现异常情况
3. 可配置性:
   - 将 BLACKLIST_PREFIX 改为可配置项，提高灵活性 
   - 考虑添加默认 TTL 值，减少调用方的负担
4. 批量操作支持:
   - 考虑添加批量添加和批量检查方法，提高处理效率